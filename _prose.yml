---
  prose:
    media: /images
    ignore:
      - /app
      - /styles
      - /tasks
      - /fonts
      - _prose.yml
      - .gitignore
      - .travis.yml
      - gulpfile.js
      - index.html
      - package.json
